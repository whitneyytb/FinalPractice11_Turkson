Practice 9: RESTful Endpoint with Express

For this assignment, I created a basic Express server with one RESTful GET endpoint.

The endpoint is:
http://localhost:3000/api/message

When the endpoint is opened in a browser or tested in Postman, it returns a JSON response with a welcome message from the API.

This practice helped me understand how Express can be used to create server-side API routes for future dashboard applications.